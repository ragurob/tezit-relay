# Server
PORT=3002
NODE_ENV=development

# Database
DATABASE_URL=file:./tezit-relay.db

# Auth (pluggable — bring your own JWT secret)
JWT_SECRET=change-me-in-production
JWT_ISSUER=tezit-relay

# Limits
MAX_TEZ_SIZE_BYTES=1048576
MAX_CONTEXT_ITEMS=50
MAX_RECIPIENTS=100
